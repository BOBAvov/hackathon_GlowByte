## ⚙️ Установка и Запуск

### 1. Запуск контейнеров
Откройте терминал в корневой папке проекта (`Byte/`) и выполните команду:

```bash
docker-compose up --build -d
```

*Флаг `-d` запускает контейнеры в фоновом режиме.*

### 2. Доступ к приложению

После успешного запуска (обычно занимает 10-20 секунд):

*   🖥 **Веб-интерфейс:** [http://localhost](http://localhost)
*   ⚙️ **API Документация (Swagger):** [http://localhost:8080/docs](http://localhost:8080/docs)

---

## 📝 Инструкция по использованию

1.  **Загрузка данных:**
    *   Откройте веб-интерфейс.
    *   На вкладке "Загрузка" перетащите три CSV файла:
        *   `weather.csv` (Погода)
        *   `supplies.csv` (Поставки/Склад)
        *   `temperature.csv` (Замеры температур)
    *   Нажмите кнопку **"Начать анализ"**.

2.  **Мониторинг:**
    *   После анализа откроется Дашборд.
    *   **Календарь:** Показывает дни с высоким риском.
    *   **Список:** Справа отображается журнал угроз.
    *   Кликните на любое событие, чтобы увидеть детали (вероятность, масса, локация).

3.  **Статистика:**
    *   Переключитесь на вкладку "Статистика" в шапке сайта, чтобы увидеть тренды и распределение рисков по типам угля.

---

## 🔧 Управление

**Остановить работу:**
```bash
docker-compose down
```

**Посмотреть логи (если что-то не работает):**
```bash
docker-compose logs -f
```
